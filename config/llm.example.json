{
  "_comment": "AI(通用模型)配置：idea2prompts 用。OpenAI 兼容 /chat/completions；enabled=true 才会真正联网。密钥勿提交(已 gitignore)。api_key 可留空：spark 本地 vLLM/Ollama 等自部署端点通常无密钥，留空时本引擎不发 Authorization 头。公网服务(如 DeepSeek)才需要填。本地 vLLM 示例见 config/llm.spark-qwen3.example.json。",
  "enabled": false,
  "kind": "openai_compatible",
  "base_url": "https://api.deepseek.com/v1",
  "api_key": "",
  "model": "deepseek-chat",
  "temperature": 0.7,
  "timeout_seconds": 120,
  "max_tokens": 500
}
