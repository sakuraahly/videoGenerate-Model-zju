# 交接文档 · 2026-09-11（创空间 + 本机修复）

> 先看三处：`docs/CURRENT-STATE.md`（事实权威）· `docs/space-manual/`（创空间说明书 8 份）· 本文（本轮交接）。

## 一、本轮做完的事（已提交并同步 spark）

### A. 创空间（魔搭）
| 项 | 状态 |
|---|---|
| 形态 | 只放 Agent、不部署模型、**不提供任何密钥**（`ALLOW_ENV_FALLBACK=0`，`LLM_API_KEY` 已删除） |
| 版本 | **v3.4** Running / public |
| 新增能力 | 服务商一键预设（百炼/DeepSeek/魔搭/智谱/自定义）、BYOK 面板、连接自测按钮、任务类工具 4 个 |
| 本地可跑 | `.env` 自动读取、三套变量名自动映射（ENGINE_* / VIDEO_API_* / REMOTE_API+STUDIO_TOKEN）、`studio/快速上手.md` |
| 安全加固 | 会话密钥 TTL 2h + 上限 200、State 只存不透明 id、SSRF 拒云元数据、任务号白名单、文件名清洗、产物目录随机化、内存上限、刷新与轮询限流、大脑调用限流、前端转义补引号 |
| 文档 | 空间仓库：`README.md`（接口配置指南）+ `接口说明.md`；**创作手记已移出空间**（只在主仓库） |

### B. 本机（spark）
| 项 | 状态 |
|---|---|
| 音色-语言错配 | `tts.match_voice_to_text()`：中文台词配中文音色（xiaoxiao/yunxi）、英文配 aria/daler、auto 按文本选 |
| Agent 规则 | 系统提示新增台词与音色铁律：台词原文走 tts_text、禁翻译、禁写进画面提示词、音色按语言选、字幕用原文 |
| 回传兜底（部分） | `h3_submit` 落盘即标成品（`_session_place(..., final=True)`） |
| 真实性校验放宽 | 任何工具的 TASK_SUBMITTED/prompt_id 都算真实提交；同会话已确认任务号复述不再判虚构 |

### C. 工作流与文档
- 成品链模板注册为 stage：`finalize`/`rife`/`restore`（实跑验证）；`t2v_ui` = 内置生成器 UI 孪生；
- 未注册模板归档 `workflows/remote_workflows/archive/`，同事原件落 `archive/originals/`（同步脚本不再覆盖在用模板）；
- `runs/h3/workflow_audit.py`（只读审计）· `runs/h3/export_ui_template.py`（导出可读回 UI 模板）；
- `docs/space-manual/`（8 份）· `docs/guides/defense-briefing.md`（答辩手册）· `zero-trust-static-agent.md` · `workflow-single-source.md`。

## 二、未完成 / 待办（最重要）

### 1. UI 反查兜底仍返回 None（本轮最后一项）
实测：
```
latest_final(Path('.'), '20260911_013310_24b9') -> None   （服务已重启，ui_http=200，成片实际已生成）
```
已实现两层：① 会话目录 → `_final.json` → 命名兜底；② 反查：会话 jsonl 抽 prompt_id → 扫 `workflows/*/job.json` → 取产物；
第三层补丁 `_product_of_task`：`job.json.videos` → 任务日志 LOCAL_OUTPUT → 任务目录 mp4 —— 仍没命中。
下一步（按序打印中间量再修）：
1. 打印 `_session_prompt_ids(repo, cid)` 的结果；
2. 打印命中的任务目录；
3. 打印该任务 `job.json.log_file`，并找该 LOCAL_OUTPUT 行实际写在哪个日志（怀疑：会话级 run 日志或更早日志，我只扫了最近 20 个 run_*.log）；
4. 验收标准：`latest_final(...)` 返回真实路径且 `is_file()` 为真。
文件：`runs/h3/session_outputs.py`（模块尾两次覆盖式补丁，注意顺序：`_latest_final_local`→`latest_final`；`_product_of_task_v1`→`_product_of_task`）。

### 2. 其他待办
- 参考引擎网关 `studio/engine_example.py`（给还没有视频接口的人一个 30–60 行示例）；
- 答辩材料：演示视频/PPT 未产出（`defense-briefing.md` 第七节有 8 分钟脚本）；
- 平台敏感内容扫描：`app.py` 被回退过一次（原因未明），复现则把 API_KEY/TOKEN 字样改成凭据变量；
- 百炼 key 轮换：曾填在公开空间，建议控制台删除/轮换。

## 三、快速验收清单

```bash
py -3 -m pytest tests -q            # 期望 299 passed / 1 skipped
python runs/h3/workflow_audit.py    # 期望 7 stage + t2v_ui 全 OK
python runs/consistency_check.py    # 期望 问题 0
# 空间配置：variables 全 unset + ALLOW_ENV_FALLBACK=0；secrets 只剩 access_token/ENGINE_API_KEY/AGENT_TOKEN（unset）
cd studio && python app.py --port 7860   # 本地跑；或写 .env 填两个 key
```

## 四、关键文件

| 位置 | 说明 |
|---|---|
| `studio/agent_client.py` | Agent 内核：8 工具、三种大脑通道、请求体构造、台账、安全工具、.env 加载 |
| `studio/app.py` | 空间前端：会话注册表（TTL/上限）、BYOK 面板、服务商预设、自测按钮 |
| `studio/backend.py` | 创作台后端（demo/remote 早期形态/local 预留） |
| `runs/h3_submit.py` | 本机唯一提交入口 |
| `runs/h3/session_outputs.py` | 会话产物与成品标记（待修的反查兜底在此） |
| `runs/agent/scheduler.py` | Agent 系统提示（台词与音色铁律） |
| `runs/agent/ui_app.py` | 7860 界面（结果区、任务面板、真实性校验） |
| `runs/h3/tts.py` | TTS 与音色语言匹配 |

## 五、本轮提交

`a62bd13` .env/别名/快速上手 · `78101d7` 回传兜底+真实性校验 · `c4902e4` 音色语言匹配 · `f1bb3ae` 台词与音色铁律 · `d18cc4a` 反查兜底（未完成）；空间侧 `4205e45`（v3.4）
