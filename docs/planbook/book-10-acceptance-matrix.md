# 计划书 — 验收矩阵 / 全局回归 / 风险回滚总表（2026-09-04 实况版）

> 状态：**按实况落勾**（✅=已闭环/🟡=部分或待小项/⬜=未开始）；证据指向对应册记录。
> 校验方式遵循 book-09（防绕过）：✅ 项均为 spark 真实执行证据或用户实测确认。

---

## 1. 阶段验收矩阵（实况）

| 阶段 | 关键验收 | 状态 | 证据 / 记录 |
|---|---|---|---|
| 0 基座 | 三端一致 + 版本指纹 + e2e_smoke SMOKE_OK + 一致性 0 问题 | ✅ | book-01 §6b；历次提交三端一致；AGENT_VERSION 均与 spark HEAD 一致 |
| 1 前端 | 新文案/继续按钮/错误分类/上下文显示/新会话清空+防误建 | ✅ | /config 实况逐串命中；book-02 §11 修复记录 |
| 2 输出 | 语言铁律（中文）+ 寒暄不续接 | ✅ | SYSTEM_MESSAGE 语言铁律；CLI「你好」只回一次（E2E）；book-03 §10 |
| 3 自动完成 | should_continue 判别（截断/任务意图才续；已提交/熔断/寒暄不续） | ✅ 核心 | 单测 8/8 UNIT_OK + CLI 同判 + E2E |
| 4 隔离 | 会话级素材隔离 + 重复登记(多会话共享) + 字面词归一 + 空会话线索与授权引导 | ✅ | 单测 14/14 + spark ISOLATION_OK + 用户实测（本会话只见本会话素材） |
| 5 工作流 | 模板/槽位属性词化 + 逐段提示词注入 + 参考图绑定 + 默认资产守卫 | ✅ 核心 | manifest per-seg distinct=2；/queue LoadImage=已绑定图；book-06 §11 |
| 6 引擎/批量 | 导入修复 + status 即时(超时对齐/终态归一) + BATCH_MANIFEST/poll_batch | ✅ 核心 | h3_batch dry-run rc=0、status 0.0s、逐段失败态正确；book-07 §10 |
| 7 风格 | 必问清单从严 + 汇报风格 + style-guide | ✅ 首批 | book-08 §10；正式回归（完整对话任务）待用户提供 |
| 8 验证 | 黄金路径闭环（绑定→提交→取片→非旧资产→产物正常） | ✅ | GOLDEN_OK（87dccd14 → video_11.mp4 521KB）；画面人工确认=正常（用户 2026-09-04） |
| 9 日志 | 全场景/无垃圾/不错失 | ⬜ | book-11（未开始；设计已就绪） |
| 10 模块化 | 注册表驱动/便捷更换工作流 | ⬜ | book-12（未开始；capabilities 雏形存在） |

---

## 2. 全局回归清单（实况勾选）

- [x] 上传大图/多图：真实上传多轮（含重复）→ 状态/预览/归属正常。
- [x] 上传幂等：重选同批 → 跳过并登记会话归属（修复批次 1/2 验证）。
- [x] 新会话/加载历史：即时；新会话清空上传预览与状态（修复批次）。
- [x] 发送清空+锁：处理中点发送被忽略（代码路径保持；未逐压测——🟡）。
- [x] 版本指纹/状态条：日志与界面头部 AGENT_VERSION；心跳显示模型/进度。
- [x] 中止按钮：文案与语义更新（停止当前任务；逐测——🟡）。
- [ ] 流式/分块（book-03 增强）：消息级分批渲染——待做（逐 token 不可行已调研；分批/打字机为路径）。
- [x] 输出截断自动续：判别函数+CLI/界面同判；寒暄不续、任务意图推进。
- [x] 多图转场（黄金路径 2 图单段）：绑定+提交+取片+画面正常✅；N 图→N-1 段真实多段：manifest/逐段注入 dry-run 验证（distinct=2）——真实多段未逐段跑（🟡 可选）。
- [x] 资源隔离：本会话互不泄露；最近线索+授权引导；未授权 all 有强警示。
- [x] 工作流提示词：模板/槽位属性词化；逐段注入；守卫拦截默认资产。
- [x] 批量/引擎：导入固定；status 不假死；逐段提示词；poll_batch 真实状态。
- [x] 错误提示：_err_hint 分类+建议（含 one-system-message 400 专门分支）。
- [ ] nap/wake：提交后让位→下轮唤醒——历次会话有发生（提交后 LLM 可用），未专项逐测（🟡）。
- [x] 产物边界：LOCAL_OUTPUT 与 outputs/ 一致（GOLDEN_OK）；不把旧片当新结果（守卫+日志）。
- [x] 异常取证：重现场景均有日志/记录（400 事故、旧图事故、跨会话引用等，均查证并回写）。

---

## 3. 风险与回滚速查（含实测补充）

| 风险 | 对策 | 实测补充 |
|---|---|---|
| 文件改写 EIO(Win32 1175) | 重试→[IO.File]::WriteAllText→先 read 再 write | 已入 dev-workflow §10，后续零阻力 |
| 子进程/字符串转义 | 行数组+完整引号；避免反斜杠/反引号多层 | 多次踩坑已固化经验（§10.4） |
| 会话隔离误伤（旧上传无 cid / 重复未登记 / cid 传不进） | 三连修复（dup 登记、Gradio State 输入注入、多会话共享）+ 空会话线索 | 用户实测过关 |
| 模型措辞（current 字面值/未授权 all） | 工具归一化+强警示+素材边界 | 已兜底；风格册继续 |
| 守卫误报 | 传图即放行；默认资产标记维护 | 已规避 |
| GPU 首载/排队（40 分钟事件） | 反馈增强（待做 book-03 增强）+ 分次取片 | 队列现已空；属环境现实非卡死 |

---

## 4. 依赖与顺序总结（按册序）

- 已按册序完成：book-01 → 02 → 03(核心) → 04 → 05(含补丁) → 06/07(核心) → 08(首批) → 09(黄金路径闭环) → 10（本册）。
- 待做（按册序）：**book-11（日志体系）→ book-12（注册表化）**；随册穿插：book-03 增强（消息级分批渲染=流式）、book-08 正式风格回归（待用户完整对话任务）。
- 里程碑：黄金路径已闭环（book-09 §6b），后续改动可用 tests/golden_path.py 重放。

---

## 5. 整件事的完成定义（实时更新）

截至 2026-09-04：两图转场黄金路径已达——绑定→提交→守卫→取片→画面正常；界面、隔离、引擎、风格首批已闭环。
整件事完成的剩余条件：
1. book-11 日志体系落地（证据源完善）。
2. book-12 注册表化（便捷更换工作流）。
3. 流式/消息级分批渲染（book-03 增强）。
4. 风格正式回归（用户提供完整对话任务）。
5. （可选）N 图 → N-1 段真实多段生成验证。

---

## 6. 阅读与校验顺序

1. book-00（总览+状态）→ book-13（待做池活文档）→ 本册（验收实况）→ 按剩余项取 book-11 / book-12 / book-03 增强。